server:
  tomcat:
    accesslog:
      directory: logs/${spring.application.name}
      enabled: true
    basedir: .

spring:
  data:
    mongodb:
      uri: "mongodb://dvm1:27017"

contagio:
  passResourcesDir: "./passresources"
  baseUrl: "https://www.contagio.de:13013"

